# Vitest always loads .env.test over .env (see Vite's env-file precedence), so the test suite
# stays pinned to the in-memory mock backend no matter what a developer has locally set
# VITE_USE_MOCK_API to in .env for their own dev-server session against the real API.
VITE_USE_MOCK_API=true
